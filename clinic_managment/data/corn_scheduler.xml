<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="ir_cron_auto_no_show" model="ir.cron">
            <field name="name">Clinic: Auto Mark Appointments as No Show</field>
            <field name="model_id" ref="model_clinic_appointment"/>
            <field name="state">code</field>
            <field name="code">model._cron_mark_no_show()</field>
            <field name="interval_number">30</field>
            <field name="interval_type">minutes</field>
            <field name="active">True</field>
        </record>

    </data>
</odoo>